# Personal Library Management

A personal library to manage books friends and loan. 

## How to run the application

This app uses [pixi](https://pixi.sh) by Prefix, be sure to have it installed it locally. After cloning the project locally run:

```bash
# install dependances
pixi install

# start the streamlit app
pixi run start

```

